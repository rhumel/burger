<ul>
	
	<input type="text" name="BurgerName" value="{{burger_name}}">
	<button class="devour-it" data-id="{{id}}" data-eaten="{{eaten}}">
		{{#if eaten}}Devour It!{{/if}}
	</button>
	{{#unless eaten}}
	<input type="text" name="delBurgerName" value="{{burger_name}}" disabled>
	{{/unless}}
</ul>
